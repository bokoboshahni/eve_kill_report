# frozen_string_literal: true

RSpec.describe EVEKillReport do
  it "has a version number" do
    expect(EVEKillReport::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
